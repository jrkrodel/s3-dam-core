import React from 'react';
import { SanityUpload } from '../types';
interface FileMetadataProps {
    file: SanityUpload;
}
declare const FileMetadata: React.FC<FileMetadataProps>;
export default FileMetadata;
